// Day 25, Minimum Cost for Tickets
#include <iostream>
#include <vector>
using std::vector;

class Solution {
    public:
        int mincostTickets(vector<int>& days, vector<int>& costs) {
            return 0;
        }
};

int main() {
    // Compile successful
    return 0;
}
