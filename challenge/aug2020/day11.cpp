// Day 11, H Index
#include <algorithm>
#include <iostream>
#include <vector>
using std::vector;

class Solution {
    public:
        int hIndex(vector<int>& citations) {
            int index;
            return index;
        }
};

int main() {
    // Compile successful
    return 0;
}
