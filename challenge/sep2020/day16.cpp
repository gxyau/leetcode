// Maximum XOR of Two Numbers in an Array
#include <iostream>
#include <vector>
using std::vector;

class Solution {
    public:
        int findMaximumXOR(vector<int>& nums) {
            int xor = 0;
            return xor;
        }
};

int main() {
    // Compile successful
    return 0;
}
