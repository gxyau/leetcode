// Day 4, Partition Labels
#include <iostream>
#include <string>
#include <vector>
using std::string;
using std::vector;

class Solution {
    public:
        vector<int> partitionLabels(string S) {
            vector<int> partitions;
            return partitions;
        }
};

int main() {
    // Compile successful
    return 0;
}
