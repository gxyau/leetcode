// Length of Last Word
#include <iostream>
#include <string>
using std::string;

class Solution {
    public:
        int lengthOfLastWord(string s) {
            int length = 0;
            return length;
        }
};

int main() {
    // Compile successful
    return 0;
}
