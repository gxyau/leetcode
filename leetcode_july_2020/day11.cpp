// Day 11, Subsets
#include <iostream>
#include <vector>
using std::vector;

class SolutionDay11 {
public:
    vector<vector<int>> subsets(vector<int>& nums) {
        vector<vector<int>> powerset;
        int n = nums.size();
        return powerset;
    }
};

int main() {
    // Compile successful
    return 0;
}
